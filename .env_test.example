# Template pour la base de données de test
# Copier en .env_test et remplir les valeurs réelles
# .env_test est dans .gitignore — ne jamais commiter les credentials

DB_HOST=localhost
DB_PORT=5432
DB_NAME=cardiolab_test
DB_USER=your_user
DB_PASSWORD=your_password
