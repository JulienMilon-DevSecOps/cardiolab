# Core
numpy
scipy
matplotlib

# Database
python-dotenv
psycopg2-binary

# Dev
pytest
pytest-benchmark
ruff

# Garmin FIT (optional — required for parse_garmin_fit / extract_training_session_garmin)
fitparse>=1.2

# Analysis (optional — required for to_dataframe() and CSV/JSON exports)
pandas>=2.0
jinja2>=3.0     # required by pandas .style (background_gradient, HTML tables)